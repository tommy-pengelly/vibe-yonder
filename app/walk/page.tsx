import App from "@/components/App";

// The active-yonder lifecycle (search → compose → walk → recap) is a full-screen
// takeover launched from the ⊕ button. It has no bottom nav.
export default function WalkPage() {
  return <App />;
}
